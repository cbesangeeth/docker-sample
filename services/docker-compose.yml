version: '3'
services:
  redis:
    image: redis
    ports:
      - "6379:6379"
  dbtest:
    image: mysql:5.7
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - "./mysql:/var/lib/mysql"
